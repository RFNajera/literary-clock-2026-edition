Pillow>=9.0.0
# Install on the Raspberry Pi to drive the panel:
#   pip3 install inky
# (Pulls in numpy, spidev, RPi.GPIO, etc. — Pi-only, so it is not required
#  for previewing frames on a desktop.)
inky>=1.5.0 ; platform_machine == "armv7l" or platform_machine == "aarch64"
